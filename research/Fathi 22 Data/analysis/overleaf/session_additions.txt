All net-new prose chunks added to paper-trim-B.tex during this session, separated by --- so you can rephrase block-by-block. 
NEW SENTENCES IN BODY TEXT The same shape still holds when conditioning on the DFS stimulus only, with G2 highest and G3 lowest (\Cref{fig:results}(a)). We combine BFS and DFS trials per-participant for all body-text statistics in this section, preserving the original within-subject design. We also report the same analysis, but restricted to the DFS subset of data (\Cref{fig:results}(b), \Cref{fig:summary}); restricting to DFS keeps the direction of substitution the same but pulls G3 from ρ=−.631 to ρ=−.660 and leaves G1 close to zero (ρ=+.013).The lead TFD scatter (in x-y space) is shown in \Cref{fig:results}(b) and \Cref{fig:summary} compounds the TFD and FC panels into one. All figure caption statistics are reproducible end-to-end from the exported Tobii .xls files through the full open regen_figures.py pipeline distributed within the supplemental materials. \Cref{fig:coupling} restricts the main effect to the DFS Metal corpus, where the sharp gradient effect shows (mean |ρ| = .368, .483, .605 for G1, G2, G3). Scanner index, fixation depth, and switching rate did not differ by group (all p>.10, Kruskal-Wallis) ruling out a global pacing explanation. NEW FIGURE CAPTIONS DFS DE Metal results. (a) Per-participant pseudocode/map TFD ratio shows the inverted-U with G2 at the apex. (b) Per-participant TFD on pseudocode vs.\ TFD on map: as expertise rises the regression slope rotates from flat (G1) to strongly negative (G3). Inverted-U in pseudocode/map TFD ratio: G2 highest, G3 lowest. Primary result: TFD substitution. G3 ρ=−.66, p=.002; G1 flat. DFS DE Metal: Spearman ρ between pseudocode and map for TFD (lead) and FC (secondary). The TFD column moves from 0 to −.66; the FC column shows the directional sign reversal noted in the paper. Asterisk marks p<.05. Pairwise |ρ| between eight gaze metrics on the DFS DE Metal stimulus. By group, per metric. Mean |ρ| rises from .368 (G1) to .483 (G2) to .605 (G3); cells in G3 panel grow darker, showing tighter inter-metric coupling at higher expertise. NEW ALT-TEXT (FOR ACCESSIBILITY) Boxplot of pseudocode-to-map TFD ratio by expertise group on the DFS Metal stimulus. Scatter plot of TFD pseudocode vs map for G1 G2 G3 on DFS Metal. Lollipop chart of Spearman rho between pseudocode and map AOIs for TFD and fixation count, by expertise group, on the DFS Metal stimulus. Three heatmaps of absolute Spearman correlations between eight key gaze metrics for groups G1, G2, and G3 on the DFS Metal stimulus. The heatmaps grow visibly darker from G1 to G3. NEW PACKAGE LINES (NOT PROSE BUT NEW) \usepackage{amsmath} \usepackage{subcaption}
